import * as PIXI from 'pixi.js';
export type PointLike = number | [number, number] | {
    x: number;
    y: number;
} | PIXI.Point | PIXI.ObservablePoint;
/**
 * Parses a value to a Point
 */
export declare function parsePoint(point: PointLike): PIXI.Point;
